import { FC } from 'react';
import PaidRecordingPage from "@/components/screens/paidRecording/paidRecordingPage";
import {ContactsArray} from "@/interfaces/contact.interface";
import {NextPage} from "next";


const PaidRecording: FC =() => {
    return <PaidRecordingPage />;
};

export default PaidRecording;

